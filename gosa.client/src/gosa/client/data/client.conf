[core]
loglevel = WARNING
log = syslog
profile = False

[client]
user = gosa
group = gosa

[amqp]
#url = %URL%

[puppet]
target = /etc/puppet
command = /usr/bin/puppet
report-dir = /var/log/puppet
manifest = %(target)s/manifests/site.pp
